name: Deploy to Test Server

on:
  push:
    branches:
      - developer

jobs:
  deploy:
    runs-on: self deploy

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: SSH Deploy to Test Server
        uses: appleboy/ssh-action@master
        with:
          host: 103.152.79.56
          username: root
          password: apt@test
          port: 22
          script: |
            cd /var/www/html/
            git pull maintestprod developer
            # Additional commands for deployment
